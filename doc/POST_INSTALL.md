The app url is <https://__DOMAIN____PATH__>

The app install dir is `__INSTALL_DIR__`

The app id is `__ID__`

**IMPORTANT**
Please go immediately on <https://__DOMAIN____PATH__> and set a user and password for admin access.
Otherwise anybody can do it.
