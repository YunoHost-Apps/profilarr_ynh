Upgrading from Profilarr v1: v2 uses an incompatible database format (confirmed by upstream, no migration exists). Existing data (database, settings, linked arr configs) will be reset. You will need to go through the setup wizard again after the upgrade.